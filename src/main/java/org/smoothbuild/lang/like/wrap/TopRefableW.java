package org.smoothbuild.lang.like.wrap;

import org.smoothbuild.lang.define.TopRefableS;
import org.smoothbuild.lang.like.common.TopRefableC;

public class TopRefableW extends RefableW implements TopRefableC {
  public TopRefableW(TopRefableS refableS) {
    super(refableS);
  }
}
